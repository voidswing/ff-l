# AI 판사 클라이언트

간단한 Flutter UI로 사연을 입력하고 서버의 판단 결과를 확인합니다.

## 실행 (Flutter 설치 필요)

```bash
cd client
flutter pub get
flutter run
```

서버가 `http://localhost:8000`에서 실행 중이어야 합니다.

안드로이드 에뮬레이터에서 실행할 경우 앱이 자동으로 `http://10.0.2.2:8000`을 사용합니다.
